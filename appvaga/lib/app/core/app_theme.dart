import 'package:flutter/material.dart';

var colorScheme = ColorScheme.fromSeed(seedColor: const Color.fromARGB(255, 0, 0, 0));

final ThemeData themeData =
    ThemeData(colorScheme: colorScheme, useMaterial3: true);
